import { mockData, render, screen } from "../../utils/test-utils"
import ItemList from "./index"

//   title: string
//   items: Item[]
//   onEquip?: (itemInventoryId: number) => void
//   equippedItems?: number[]

describe("ItemList - Component", () => {
  it("should select item and show it when clicking on one", () => {})
  it("should show all items's image of items passed in props", () => {})
})
